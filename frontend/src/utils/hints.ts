/**
 * Пояснения к показателям интерфейса.
 *
 * Формулировка каждого показателя живет здесь в единственном экземпляре: если
 * один и тот же показатель объясняется на разных экранах по-разному, читатель
 * решает, что это разные показатели.
 */

/** Что вообще означает оценка качества. Входит во все пояснения к ней. */
const QUALITY_MEANING =
  'Оценка качества (0–100) показывает, насколько надежны данные, на которых построен ' +
  'расчет. Это характеристика данных, а не выгодности поездки: высокая оценка означает, ' +
  'что стоимость собрана из достаточного числа согласованных и свежих предложений.';

const QUALITY_FACTORS =
  'Складывается из шести факторов: полнота компонентов (30 %), число источников (20 %), ' +
  'число предложений (15 %), согласованность источников (15 %), свежесть данных (10 %) ' +
  'и стабильность коннекторов (10 %). Расчет с оценкой ниже 60 считается частично успешным.';

/** Полное пояснение: там, где оценка качества — самостоятельный блок экрана. */
export const QUALITY_SCORE_HINT = `${QUALITY_MEANING} ${QUALITY_FACTORS}`;

/** Краткое пояснение: колонки таблиц и другие тесные места. */
export const QUALITY_SCORE_SHORT_HINT =
  `${QUALITY_MEANING} Учитываются полнота компонентов, число и согласованность источников, ` +
  'объем предложений, свежесть данных и стабильность коннекторов.';

export const MIN_QUALITY_SCORE_HINT =
  'Отсекает расчеты, у которых оценка качества ниже заданной; 0 — не фильтровать. ' +
  QUALITY_MEANING;

export const AVG_QUALITY_SCORE_HINT =
  'Среднее арифметическое оценок качества по всем расчетам выборки. ' + QUALITY_MEANING;

export const KPI_STABILITY_HINT =
  'Доля расчетов со статусом «Успешно» либо «Частично», у которых оценка качества выше ' +
  'порога профиля. Целевое значение — не менее 80 %.';

export const UNREPORTED_ATTRIBUTES_HINT =
  'Признаки, которых нет в контракте источника: он их не возвращает в принципе. ' +
  'Такой пробел известен заранее и не приравнивается к сбою классификации, поэтому ' +
  'не снимает источник с расчета по доле неклассифицированных предложений. ' +
  'На фильтр сценария это не влияет: предложение с неизвестным признаком по-прежнему ' +
  'не выдается за соответствующее запросу.';

/** Доверие к источнику намеренно отделено от оценки качества расчета. */
export const SOURCE_CONFIDENCE_HINT =
  'Долгосрочная оценка доверия к источнику как поставщику данных (0–100). Это не ' +
  'техническая доступность и не оценка качества отдельного расчета. Влияет на уверенность ' +
  'в результате, но не используется как скрытый вес цены.';
